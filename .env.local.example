# Copy this file to .env.local and fill in real values for local testing.
# In GitHub Actions, these are set as repository Secrets instead — never commit real values.

ANTHROPIC_API_KEY=
API_FOOTBALL_KEY=
WP_URL=https://thefootball.report
WP_USERNAME=
WP_APP_PASSWORD=
